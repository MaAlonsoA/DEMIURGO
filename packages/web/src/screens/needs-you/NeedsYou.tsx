// Needs you: placeholder until cut 5 of the spec.

import { EmptyState, Page, PageTitle } from '../../ui/layout.tsx';

export function NeedsYouScreen() {
  return (
    <Page>
      <PageTitle title="Needs you" />
      <EmptyState>This screen arrives in cut 5.</EmptyState>
    </Page>
  );
}
