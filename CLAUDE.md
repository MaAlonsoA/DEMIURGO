# CLAUDE.md

Lee primero `AGENTS.md`: contiene las reglas de fondo de la v2 (aceptación solo humana, actor fijado por el servidor, tablas como datos, diario append-only) y las prohibiciones de entorno (`demiurgo-stable`, puerto 8000).

La v2 se está construyendo en la rama `v2`. La estructura, los comandos y la arquitectura se documentan aquí a medida que existen.
