// New version: placeholder until cut 4 of the spec.

import { EmptyState, Page, PageTitle } from '../../ui/layout.tsx';

export function NewVersionScreen() {
  return (
    <Page>
      <PageTitle title="New version" />
      <EmptyState>This screen arrives in cut 4.</EmptyState>
    </Page>
  );
}
