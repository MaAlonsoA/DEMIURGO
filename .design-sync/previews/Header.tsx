import * as React from 'react';
import { Header } from '@demiurgo/design-system';

export const Product = () => (
  <div style={{ width: 640 }}><Header project="DEMIURGO v2" tabs={['Product', 'Threads', 'Knowledge']} current="Product" needs={2} /></div>
);
