# S0 · Ejecución real de `eco` a través del motor durable

Fecha: 2026-09-24, 13:19 UTC. Cumple AC-ESQ-001-16 (manual): una ejecución real con Claude registrada con su resultado.

## Cómo se lanzó

Base dedicada `demiurgo_v2_ejecuciones_reales` en el Postgres de desarrollo (no es una base en uso ni la de la instancia estable):

```powershell
$env:DEMIURGO_DATABASE_URL = 'postgres://demiurgo:demiurgo-dev@127.0.0.1:55432/demiurgo_v2_ejecuciones_reales'
$env:DEMIURGO_AGENTE = 'claude'; $env:DEMIURGO_MODELO_AGENTE = 'haiku'
node packages/api/src/cli.ts crear-proyecto "Ejecuciones reales S0"
node packages/api/src/cli.ts ejecucion-real <proyecto> eco '{"tipo":"proyecto"}' '{"texto":"Primera ejecución real de DEMIURGO v2 a través del motor durable"}'
```

Recorrido: `run.request` (actor `system:cli@1`) construye el context pack `eco@1` y encola la ejecución → el flujo DBOS `demiurgo.run` hace `run.begin`, invoca `claude -p` con `--json-schema` en un directorio temporal vacío y sin herramientas, valida la salida con el esquema Zod de `eco` y aplica `run.complete` en la misma transacción que la marca de paso completado.

## Resultado

| Campo | Valor |
|---|---|
| Estado | `completed` |
| Proveedor y modelo observado | `claude-cli` · `claude-haiku-4-5-20251001` |
| Método y esquema | `eco@v1` · `b1966325f2bc02a3` |
| Salida validada | `{"reply": "Primera ejecución real de DEMIURGO v2 a través del motor durable."}` |
| Tokens | 1489 de entrada, 308 de salida |
| Duración del agente | 3873 ms (4,2 s del flujo completo) |
| Coste declarado | 0,0030 USD (suscripción: no se factura aparte) |

## Observaciones

- La salida cumple el esquema a la primera; no hubo reintentos.
- El arranque de la CLI de Claude añade unos 2 s fijos por llamada: con un único ítem, la latencia está dominada por el arranque, no por el modelo.
