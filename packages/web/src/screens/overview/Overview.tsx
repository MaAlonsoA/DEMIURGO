// Overview: placeholder until cut 2 of the spec.

import { EmptyState, Page, PageTitle } from '../../ui/layout.tsx';

export function OverviewScreen() {
  return (
    <Page>
      <PageTitle title="Overview" />
      <EmptyState>This screen arrives in cut 2.</EmptyState>
    </Page>
  );
}
